<div class="row">
  <div class="col-2 d-flex justify-content-center <%= screen.include?('answer') ? 'bg-blue' : 'bg-pink' %> py-3">
    <div class="row align-content-center ">
      <p class="text-center m-0 text-white fs-5 fw-bold"><%= screen.include?('answer') ? 'お題' : '採点' %></p>
    </div>
  </div>
  <div class="col-10 bg-dark-blue py-3">
    <p class="m-0 text-white fs-5 fw-bold"><%= question.content %></p>
  </div>
</div>
